# PNM-candidate-tracker

`TODO` write more info here ...

## Project Structure

```text
project-root/
├── cmd/                        # Main entry point for the bot
│   └── bot/
│       └── main.js             # Starts the Discord bot
├── src/                        # Source files for your application
│   ├── api/                    # API-related files
│   │   ├── index.js            # Express server setup
│   │   └── feedback.js         # API routes for feedback
│   ├── models/                 # Data models
│   │   └── feedback.js         # Feedback model
│   ├── services/               # Business logic for handling operations
│   │   └── feedbackService.js  # Service to handle feedback logic
│   └── config/                 # Configuration files
│       └── config.js           # Environment and config setup
├── package.json                # Node.js project metadata and dependencies
├── .env                        # Environment variables (tokens, DB credentials)
└── README.md                   # Project documentation
```
